<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production: Pin certificates for API domain -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">api.statusmy.com</domain>
        <domain includeSubdomains="true">statusmy.com</domain>
        <pin-set expiration="2026-12-31">
            <!-- Replace with actual SHA-256 pin hashes of your server certificates -->
            <!-- Generate with: openssl s_client -connect api.statusmy.com:443 | openssl x509 -pubkey -noout | openssl pkey -pubin -outform der | openssl dgst -sha256 -binary | openssl enc -base64 -->
            <pin digest="SHA-256">REPLACE_WITH_PRIMARY_PIN_HASH</pin>
            <pin digest="SHA-256">REPLACE_WITH_BACKUP_PIN_HASH</pin>
        </pin-set>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Development: Allow localhost and cleartext for dev -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">192.168.0.0/16</domain>
    </domain-config>

    <!-- Base config: system CAs only -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
